---
alwaysApply: false
globs: "**/frontend/**,**/*.vue"
---
**前端开发必须按照原型图进行开发，尤其是页面规范，不可随意设计**
# 前端开发规范

## 技术栈

Vue 3 + TypeScript + Pinia + Vue Router

## 目录结构

```
frontend/src/
├── components/     # 通用组件（AppHeader.vue, UserAvatar.vue）
├── pages/          # 页面级组件（LoginPage.vue, DashboardPage.vue）
├── stores/         # Pinia 状态管理
├── services/       # API 调用封装
├── router/         # 路由配置
├── types/          # TypeScript 类型定义
└── utils/          # 工具函数
```

## 命名规范

| 类型 | 规则 | 示例 |
|------|------|------|
| 页面组件 | PascalCase + Page 后缀 | `LoginPage.vue`, `ItemListPage.vue` |
| 通用组件 | PascalCase + 功能前缀 | `AppHeader.vue`, `UserAvatar.vue` |
| Store 文件 | camelCase | `useAuthStore.ts`, `useItemStore.ts` |
| Service 文件 | camelCase | `authService.ts`, `itemService.ts` |
| 类型文件 | camelCase | `auth.ts`, `item.ts` |

## 环境变量

前端环境变量放在 `frontend/.env`，**必须以 `VITE_` 前缀**，否则打包后不可用：

```env
VITE_API_BASE_URL=http://localhost:8000/api
```

```typescript
// 使用
const baseURL = import.meta.env.VITE_API_BASE_URL
```

> 前后端各自独立 `.env`，不要混用。

## API 调用

```typescript
// services/api.ts
import axios from 'axios'

const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api',
  timeout: 10000,
})

// 请求拦截：自动附加 Token
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截：统一错误处理
api.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)

export default api
```

## 路由守卫

需要登录的页面必须配置路由守卫：

```typescript
// router/index.ts
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  if (to.meta.requiresAuth && !token) {
    next('/login')
  } else {
    next()
  }
})
```

路由定义中用 `meta.requiresAuth` 标记需要登录的页面。

## 进入前端开发：生成 plan.md

进入开发模式后，第一件事是阅读 PRD 并生成 `docs/plan.md`（初始只填前端部分）：

```markdown
# 开发计划

## 一、前端开发

### 基础设施
- [ ] 项目初始化（Vue 3 + TypeScript + 路由 + 布局组件）

### 页面清单
#### 页面 1：[页面名]
- 功能：[该页面包含的功能点]
- Mock 数据：[列出该页面需要的 Mock 数据内容]
- 跳转：[从哪来、到哪去]
- [ ] 开发完成

#### 页面 2：[页面名]
- 功能：...
- Mock 数据：...
- 跳转：...
- [ ] 开发完成

### 前端验收
- [ ] 用户启动验收通过

## 二、后端开发（前端验收通过后补齐）
> 待前端验收通过后，按业务功能拆解填写
```

plan.md 是用户校对和模型校正的共同依据，页面功能、Mock 数据、跳转逻辑必须写清楚。完成一项勾选一项。

### plan.md 维护规范

- 开发前先在 plan.md 中写明要开发的页面清单，确认无误后再动手编码
- 前端因为已有原型图做过用户参考和选型，可以一次性开发所有页面，最终统一验收
- 每完成一个页面，在 plan.md 中勾选对应项

## Mock 数据规则

- 先用 Mock 数据把所有页面做出来，用户能看到完整交互
- **所有 Mock 数据在页面上显示时必须带[Mock]标识**，让用户能明确区分哪些是假数据
- 后端对接后，Mock 数据和标识自然替换为真实数据

## 前端开发节奏

```
基础设施（项目初始化、路由、布局）
  → 全部页面开发（Mock 数据）
  → 用户启动验收（提供启动引导）
  → 用户确认满意后进入后端开发
```

**禁止**：跳过用户验收直接开发后端

## 原型参考（强制）

开发前端页面时，**必须先读取 `.output/` 下的原型文件作为视觉参考**：
- Stitch 模式：读取 `.output/prototypes/NN-界面名/index.html` 中的布局和样式
- Pencil 模式：使用 Pencil MCP 工具的 `batch_get` 和 `get_screenshot` 查看 `.output/*.pen` 原型图

每个页面的布局结构、组件位置、配色、间距必须与原型保持一致。偏离原型需向用户说明原因并获得确认。

## 开发顺序

```
1. 路由配置（含守卫）
2. 页面骨架
3. API Service 对接
4. 状态管理（Pinia store）
5. 组件拆分与优化
```

## 易错点

| 错误写法 | 正确写法 |
|---------|---------|
| `import.meta.env.API_URL` | `import.meta.env.VITE_API_URL`（必须 VITE_ 前缀）|
| 后端前端共用一个 `.env` | 各自独立 `.env` |
| 在组件里直接 `axios.get()` | 封装到 `services/` 里统一调用 |
| baseURL 硬编码 `http://localhost:8000` | 从 `import.meta.env` 读取 |
| 401 在每个页面单独处理 | 在 axios 响应拦截器中统一处理 |

1. 多栏布局中，**不能让 grid/flex 的默认等高行为被单栏内容增长触发，进而撑高整行、破坏页面结构**；工作台页面应固定视口高度，并让各栏通过 min-height: 0 和独立 overflow 自行滚动。
2.**列表型侧栏不能依赖会被拉伸或分配剩余空间的默认布局**；列表容器必须显式顶部对齐，保证内容从上往下稳定排布。
3.前端开发完毕之后创建**backend文件夹**，后端开发进入这个文件夹中进行