---
alwaysApply: false
globs: "**/backend/**"
---

# 后端开发总规则

本文件在编辑 backend/ 下任何文件时自动注入。根据当前开发阶段，执行对应章节的规则。

---

## 阶段判断

通过检查项目状态判断当前处于后端开发的哪个阶段：

- `.output/plan.md` 中没有"二、后端开发"章节 → **初始化阶段**（执行下方「首次进入后端开发」）
- `.output/plan.md` 中有后端章节且基础设施未勾选 → **基础设施阶段**
- `.output/plan.md` 中基础设施已勾选 → **功能开发阶段**（执行下方「逐功能开发」）

---

## 首次进入后端开发（仅执行一次）

以下步骤在后端开发启动时执行，完成后不再重复。

### 1. 确认 Python 版本

询问用户当前使用的 Python 版本，确认是否满足 3.11+ 要求。如不满足，引导用户升级后再继续。

### 2. 讨论 .env 配置

列出后端需要的所有配置项，让用户逐项确认值。示例：

```env
# 数据库
DATABASE_URL=sqlite+aiosqlite:///./app.db

# 服务
HOST=0.0.0.0
PORT=8000
DEBUG=true

# CORS
CORS_ORIGINS=["http://localhost:5173"]

# AI/LLM（如需要）
LLM_API_KEY=
LLM_MODEL_NAME=
LLM_BASE_URL=
```

**根据项目实际需求列出配置项，上面只是示例。未经用户确认配置，不得开始后端编码。**

### 3. 补齐 plan.md 后端部分

阅读 PRD，**按业务逻辑（不是按页面）拆解功能清单**。先梳理功能之间的依赖关系，从最底层被依赖的功能开始排列，确保每个功能开发时它依赖的功能已经完成。

**拆解原则：**
- 按业务能力拆，不按前端页面拆。一个页面可能涉及多个业务功能，一个业务功能也可能服务多个页面
- 先列出所有功能，再标注依赖关系，最后按依赖拓扑排序
- 最底层（无依赖、被其他功能依赖的）排最前面

**示例：电商系统的功能排列**
```
错误（按页面拆）：首页功能 → 商品页功能 → 购物车页功能 → 订单页功能
正确（按业务依赖拆）：用户认证 → 商品管理 → 购物车 → 订单（依赖前三者）→ 支付（依赖订单）
```

补齐 `docs/plan.md`：

```markdown
## 二、后端开发

### 基础设施
- [ ] db/session.py、main.py、.env、requirements.txt

### 功能 1：[最底层功能名]（无依赖）
- 依赖：无
- [ ] models/（Pydantic 请求/响应）
- [ ] db/models.py（ORM 表结构）
- [ ] repositories/（数据访问层）
- [ ] services/（业务逻辑）
- [ ] api/routes/（API 路由）
- [ ] 前端对接（替换 Mock 数据为真实 API）
- [ ] 提供测试引导 → 用户测试通过

### 功能 2：[功能名]（依赖功能 1）
- 依赖：功能 1
- [ ] ...

### 功能 3：[功能名]（依赖功能 1 + 功能 2）
- 依赖：功能 1、功能 2
- [ ] ...
```

完成以上 3 步后，有了初版plan.md之后，**向用户发起确认请求**，确认用户同意按照plan.md进行开发，再进入开发后端阶段。

---

## 逐功能开发（日常执行）

### plan.md 维护规范

后端与前端不同：前端有原型图做过用户参考和选型，可以一次性开发；
**后端必须逐功能开发、逐功能让用户测试**，因为用户需要通过日志和接口测试明确当前开发进度。

**规则：**
1. **开发前必须先维护 plan.md**：在动手写任何代码之前，确认 plan.md 中当前功能的子项清晰完整
2. **严格按依赖顺序推进**：先开发被依赖的底层功能，后开发依赖它的上层功能。开发完成一个功能**就要停下请求用户测试**
完成功能 A 全部层 → 提供测试引导 → 用户测试通过 → 才能开发功能 B
3. **每完成一个子项立即勾选**：保持 plan.md 实时反映开发进度
4. **禁止**：不维护 plan.md 就开始编码、多个功能同时开发、未经用户测试就进入下一个功能、跳过底层功能先开发上层功能
5. 开发的时候必须**提供充足的日志信息**，可让用户根据日志追踪后端进度
### 分层开发顺序

每个功能内部按从下往上的顺序开发：

```
models/         →  定义请求/响应数据结构（Pydantic）
db/models.py    →  定义数据库表结构（ORM）
db/session.py   →  数据库连接（基础设施阶段写一次）
repositories/   →  数据访问层（封装增删改查）
services/       →  业务逻辑层（核心，处理业务规则）
api/deps.py     →  依赖注入（串联各层）
api/routes/     →  路由层（接收请求、调用 service、返回响应）
```

**原则**：每一层只依赖它下面的层，不得反向依赖。

### 开发节奏

```
功能 A 全部层 → 前端对接 → 提供测试引导 → 用户测试通过
  → 功能 B 全部层 → 前端对接 → 提供测试引导 → 用户测试通过
  → ...
```

### 测试引导格式

每个功能完成后，提供以下信息交给用户自行测试：
- 启动命令
- 测试步骤（具体操作：访问什么页面、点击什么按钮、输入什么数据）
- 预期结果（应该看到什么）
- 相关接口的 curl 命令（可选，方便用户直接测试 API）

**禁止代替用户执行测试。**

---

## 交付

全部功能用户测试通过后，输出 `docs/startup.md`（环境要求、启动命令、默认配置）。
