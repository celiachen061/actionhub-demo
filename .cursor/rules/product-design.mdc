---
alwaysApply: false
globs: "**/.output/**"
---

# 产品设计流程（阶段 A → B → C）
**如果你看到这段内容 回复中开头出现 【我来设计了】**
> 本文件在阶段 R 产出 `.output/` 文件后自动激活。阶段 R 定义在 core.mdc 中。

## 阶段 A：产品路线图与 PRD 初稿

通过对话输出 Product Roadmap，必须包含：
- 核心目标 (Mission)、用户画像 (Persona)
- V1: MVP 核心功能、V2+ 未来版本
- 关键业务逻辑 (Business Rules)、数据契约 (Data Contract)

### 页面梳理（必须与用户讨论确认）

在输出 PRD 初稿前，先与用户对齐：
1. 总共有几个页面
2. 每个页面包含哪些功能
3. 页面之间的跳转逻辑
4. 关键功能的完整技术实现思路（细节）
### 数据契约确认（必须用户逐项打勾）

PRD 初稿中所有影响流程结果的数据（参数细节）定义，必须以 checklist 形式列出，由用户逐项确认。示例：

```markdown
### 数据契约确认清单

#### 商品分类体系
- [ ] 一级分类：服装、数码、食品、家居（共 4 类）
- [ ] 二级分类：服装-男装、服装-女装、数码-手机、数码-电脑...（共 15 类）

#### 用户画像字段
- [ ] 昵称（string）、注册时间（datetime）、会员等级（枚举：普通/白银/黄金）
- [ ] 累计消费金额（float）、收货地址（string）、偏好品类（枚举）

#### 订单状态流转
- [ ] 状态：待支付 → 已支付 → 已发货 → 已签收 → 已完成
- [ ] 异常状态：已取消、退款中、已退款

#### 权限角色
- [ ] 管理员：全部权限
- [ ] 运营：商品管理、订单管理、数据查看
- [ ] 普通用户：浏览、下单、查看订单
```

**规则**：根据项目实际情况列出所有数据契约，上面只是示例。用户全部打勾后才能输出 PRD 初稿。

输出到：`.output/PRD-draft.md`、`.output/roadmap-draft.md`
**PRD-draft.md要求** : 
1.梳理所有页面功能及跳转逻辑
2.梳理所有复杂功能的业务链路（精细） 示例：
```markdown
### 召回链路
召回链路中，使用什么方式召回？关键词还是向量？如果是向量，使用什么模型？
```
3.所有在PRD-draft阶段梳理清楚的细节问题，都要落在最终版PRD.md中

需要请求用户确认PRD-draft中的**功能是否理解完善**以及**数据契约是否符合预期**/roadmap-draft中的**版本规划理解有没有错误**，发起确认：「是否进入原型设计阶段？」

> 未经用户确认，不得进入阶段 B。

## 阶段 B1：UI 设计说明书

**在调用 Pencil 画任何东西之前，必须先生成设计说明书。** PRD 只说"做什么"，设计说明书说"每个界面长什么样"。

### 流程

1. 根据 PRD-draft.md 列出所有 MVP 界面清单

2. **风格调研（使用 ui-ux-pro-max skill）**

   根据项目特性，使用 `ui-ux-pro-max` skill 按以下顺序搜索，收集风格参考：

   1. **product** domain → 产品类型关键词，获取推荐风格方向
   2. **style** domain → 按推荐结果深入查询 2-3 种风格细节
   3. **color** domain → 获取配色方案（行业/产品关键词）
   4. **landing** domain → 如果是落地页，搜索页面结构
   5. **chart** domain → 如果有图表/仪表盘，搜索图表类型

   **搜索结果必须写入设计说明书的「风格调研摘要」章节**，包括：
   - 推荐风格名称及特征描述
   - 推荐配色方案（Primary / Secondary / CTA / Background / Text / Border）
   - 关键 UX 规则
   - UI布局规范

3. 基于调研结果，确定 **1 种视觉风格方向**（由项目特性和调研结果决定）。

4. **为每个界面撰写详细的 UI 设计说明**，ui-design-spec.md 整体结构示例如下（可适当填充更多细节）：

```markdown
## 风格调研摘要

> 由 ui-ux-pro-max 搜索结果整理而来

### 视觉风格
- [风格名称] — [特征描述、配色、圆角、阴影等]

### 配色方案
| 用途 | 色值 |
|------|------|
| Primary | #xxx |
| Secondary | #xxx |
| CTA | #xxx |
| Background | #xxx |
| Text | #xxx |
| Border | #xxx |

### 字体搭配
- 标题 [字体] / 正文 [字体]
- Google Fonts import: [链接]

### 关键 UX 规则
- [从 ux domain 搜索结果中提取的 3-5 条关键规则]

---

## 界面清单

### 界面名称：[名称]

#### 布局结构
...（同下方模板）
```

每个界面的详细说明模板：

```markdown
### 界面名称：[名称]

#### 布局结构
- 整体布局：[左右分栏 / 上下结构 / 三栏 等]
- 左侧/顶部：[导航内容，具体菜单项]
- 主内容区：[具体放什么组件]
- 右侧/底部：[辅助信息]

#### 组件清单
| 区域 | 组件类型 | 内容/数据 | 交互行为 |
|------|---------|----------|---------|
| 顶部导航 | 水平菜单 | 首页、商品、订单、个人中心 | 点击切换页面 |
| 左侧筛选 | 筛选面板 | 品类、价格区间、评分 | 勾选后刷新列表 |
| 主区域 | 商品卡片列表 | 图片、名称、价格、评分 | 点击进入详情 |
| ... | ... | ... | ... |

#### 示例数据
- 列表项至少 3 条具体数据（不要用"数据1、数据2"占位）
- 表格至少 3-5 行真实字段
```

5. 输出到：`.output/ui-design-spec.md`
6. 发起确认：「UI 设计说明书已完成，请审阅。确认后进入原型设计阶段。」

> 未经用户确认，不得进入阶段 B2。

---

## 阶段 B2：原型设计

**严格按照 ui-design-spec.md 中的描述设计，不得自由发挥。**

### 工具选择（必须询问用户）

**进入 B2 时必须先询问用户**：「请选择原型设计工具：Stitch 还是 Pencil MCP？」**未经用户明确回答，不得开始设计。**

| 方式 | 工具 | 输出格式 | 特点 |
|------|------|---------|------|
| **Stitch** | Google Stitch | HTML + TailwindCSS（.zip） | 速度快，输出代码可直接参考开发 |
| **Pencil MCP** | 内置 Pencil | .pen 文件 | 无需外部工具，全在编辑器内完成 |

---

### 模式 A：Stitch

#### 步骤 1：生成 Stitch 提示词

**整体生成**，每个界面基于 B1 阶段确定的风格生成 **1 段英文提示词**，最后**整合输出最终提示词**。

AI 阅读 `ui-design-spec.md`，为每个界面生成英文提示词，必须包含以下内容：

```
1. 页面类型与用途
   → 从界面名称和布局结构提取

2. 视觉风格
   → 从风格调研摘要中提取：风格名称、特征描述
   → 明确指定：配色（Primary/Secondary/CTA/Background/Text/Border 色值）
   → 明确指定：字体（标题字体 + 正文字体）
   → 明确指定：圆角、阴影、间距等视觉特征

3. 布局结构
   → 从该界面的「布局结构」章节提取
   → 如：左侧边栏 240px + 主内容区，顶部导航栏...

4. 组件清单及细节
   → 从该界面的「组件清单」表格逐项提取
   → 每个组件包含：位置、类型、具体内容/数据、交互行为

5. 示例数据
   → 从该界面的「示例数据」章节提取
   → 列表项至少 3 条真实数据
   → 对话至少 2-3 轮完整内容
   → 表格至少 3-5 行

6. 视口
   → 桌面端 1440×900（Web）或 移动端 390×844（移动端）
```

**提示词模板**：

```
Design a [page type] for [product description].

**Visual Style**: [Style name] — [Style characteristics].
Colors: Primary [#xxx], Secondary [#xxx], CTA [#xxx], Background [#xxx], Text [#xxx], Border [#xxx].
Typography: Headings use [font], body text uses [font].
[Border radius, shadows, spacing details]

**Layout**: [Layout description from spec]

**Components**:
- [Area]: [Component type] — [Specific content and data]
- [Area]: [Component type] — [Specific content and data]
- ...

**Sample Data**:
- [Real data 1]
- [Real data 2]
- [Real data 3]

Desktop viewport, 1440×900.
```

将生成的提示词输出到 `.output/stitch-prompts.md`，格式：

```markdown
# Stitch 提示词

> 基于确定的风格：[风格名称]

## 界面 1：[界面名]
> [提示词内容]

## 界面 2：[界面名]
> [提示词内容]

...
```

告知用户：「已生成 Stitch 提示词，请复制到 Stitch 中生成界面。」

#### 步骤 2：用户在 Stitch 中生成并导出

用户操作（AI 给出指引）：
1. 打开 [stitch.withgoogle.com](https://stitch.withgoogle.com)
2. 粘贴提示词 → 生成界面
3. 点击 More → Download 下载 .zip
4. 解压到 `.output/prototypes/` 下对应目录

**文件规范**：

```
.output/prototypes/
├── 01-登录页/
│   ├── index.html
│   └── style.css (TailwindCSS)
├── 02-仪表盘/
│   └── ...
└── ...
```

命名规则：`NN-界面名/`，NN 为两位序号。

#### 步骤 3：AI 验证

每个界面导入后，AI 读取 HTML/CSS 文件：
- 对照 ui-design-spec.md 的组件清单逐项核对
- 检查示例数据完整性
- 检查布局结构是否与 spec 一致
- 标注缺失或偏差，反馈修改建议（用户可回 Stitch 调整后重新导出）

逐界面确认：「界面 [N/总数]『[界面名]』已验证，请确认或修改。」

#### 步骤 4：完成收尾

1. 全部界面完成后，输出 `.output/Prototype.md` 汇总说明（包含每个目录路径和对应界面）
2. 发起最终确认：「所有界面原型已完成，确认后进入阶段 C。」

---

### 模式 B：Pencil MCP

#### 准备工作

1. 阅读 `ui-design-spec.md` 中的风格调研摘要，提取配色、字体、风格关键词
2. 调用 `get_guidelines("web-app")` 获取 Pencil 设计指南
3. 调用 `get_style_guide_tags` → `get_style_guide(tags)` 获取风格参考
4. 调用 `open_document("new")` 创建新画布

#### 逐界面绘制

**每轮只画 1 个界面。**

1. 阅读 ui-design-spec.md 中该界面的描述
2. 创建 1 个 Frame（`界面名`），尺寸 1440×900
3. 严格按组件清单和示例数据绘制
4. 用 `get_screenshot` 截图验证 → 对照说明书逐项核对
5. 发起确认 → 提醒用户 ⌘+S 保存

#### 完成收尾

1. 输出 `.output/Prototype.md` 汇总说明
2. 提醒用户将 .pen 文件保存到 `.output/`
3. 发起最终确认

---

### 质量要求（两种模式通用）

- 每个界面生成 1 版可视化原型
- 列表/卡片 1 条示例数据
- 对话界面至少 1 轮气泡消息
- 禁止用 Markdown 文字描述充当原型
- 禁止不看设计说明书直接画

输出到：
- Stitch 模式：`.output/stitch-prompts.md` + `.output/prototypes/NN-界面名/`（HTML+CSS）+ `.output/Prototype.md`
- Pencil 模式：`.output/*.pen` + `.output/Prototype.md`

> 未经用户确认，不得进入阶段 C。

## 阶段 C：最终 PRD 定稿

在用户选定原型后，生成最终 PRD，内容必须与已确认原型一致，并补充：
- 核心流程图（Mermaid：sequenceDiagram 或 flowchart）
- 组件交互说明（影响模块、拟新增模块、调用关系）
- 技术选型与风险（关键库/方案、风险与缓解）

输出到：
- `.output/PRD.md`
- `.output/roadmap-final.md`
- `.output/prototype-final.md`
- `.output/architecture-blueprint.md`

### PRD 自检（生成后必须逐项核对）

- [ ] 没有"待定"、"TBD"、"后续补充"等占位符
- [ ] 每个功能模块都有明确的优先级（MVP / V2+）
- [ ] 核心流程图覆盖了主流程和至少一条异常分支
- [ ] 技术选型已确定，没有"A 或 B 待定"
- [ ] PRD 内容与已确认的原型一致，没有原型里有但 PRD 里没提的功能

自检不通过 → 修复后重新输出，不得进入开发。

